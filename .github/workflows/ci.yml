# .github/workflows/ci.yml
name: Java CI with Gradle

on:
  push:
    branches: [ "master", "develop" ] # 메인이나 데브 브랜치에 푸시될 때 실행
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest # 리눅스 환경에서 실행

    steps:
      - uses: actions/checkout@v4 # 코드 가져오기

      - name: Set up JDK 17 # 본인의 Java 버전과 맞추세요
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew build # 👈 여기서 우리가 만든 모든 테스트가 실행됩니다!

      # 테스트 결과 리포트를 업로드 (선택 사항)
      - name: Upload Test Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: build/test-results/test/